import baseStyled, {
  ReactNativeStyledInterface
} from "styled-components/native";
import { AppTheme } from "../../store/theme/types";

export const styled: ReactNativeStyledInterface<AppTheme> = baseStyled;
